<Window x:Class="CyberSecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Bot - PROG6221 Part 3" 
        Height="780" Width="1250"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E">

    <Grid>
        <!-- Menu -->
        <Menu Height="32" Background="#FF2D2D30" Foreground="White" VerticalAlignment="Top">
            <MenuItem Header="File">
                <MenuItem Header="New Task" Click="MenuNewTask_Click"/>
                <MenuItem Header="Start Quiz" Click="MenuStartQuiz_Click"/>
                <Separator/>
                <MenuItem Header="Exit" Click="MenuExit_Click"/>
            </MenuItem>
            <MenuItem Header="Tools">
                <MenuItem Header="Refresh All" Click="MenuRefreshAll_Click"/>
                <MenuItem Header="Clear Log" Click="MenuClearLog_Click"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="How to Use" Click="MenuHowToUse_Click"/>
                <MenuItem Header="About" Click="MenuAbout_Click"/>
            </MenuItem>
        </Menu>

        <Grid Margin="0,32,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Background="#FF252526" BorderBrush="#FF007ACC" BorderThickness="0,0,1,0">
                <StackPanel Margin="12">
                    <TextBlock Text="NAVIGATION" FontSize="18" FontWeight="Bold" Foreground="#FF007ACC" Margin="8,15"/>
                    <Button Content="💬 Chatbot" Height="55" Margin="8" Click="NavChat_Click"/>
                    <Button Content="✅ Tasks" Height="55" Margin="8" Click="NavTasks_Click"/>
                    <Button Content="🎮 Quiz" Height="55" Margin="8" Click="NavQuiz_Click"/>
                    <Button Content="📜 Activity Log" Height="55" Margin="8" Click="NavLog_Click"/>
                </StackPanel>
            </Border>

            <!-- Content -->
            <Grid Grid.Column="1" Margin="15">
                <Border Height="75" Background="#FF007ACC" VerticalAlignment="Top">
                    <TextBlock Text="🛡️ CYBERSECURITY AWARENESS BOT" FontSize="28" FontWeight="Bold" 
                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Chat -->
                <Grid x:Name="ChatPanel" Margin="0,85,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <RichTextBox x:Name="rtbChat" IsReadOnly="True" Background="#FF1E1E1E" Foreground="#E0E0E0" Padding="15"/>
                    <Grid Grid.Row="1" Margin="0,12,0,0">
                        <TextBox x:Name="txtInput" Height="55" FontSize="15" Background="#FF333333" Foreground="White" KeyDown="TxtInput_KeyDown"/>
                        <Button Content="Send" Width="130" Height="55" Margin="10,0,0,0" Background="#FF007ACC" Foreground="White" Click="BtnSend_Click" HorizontalAlignment="Right"/>
                    </Grid>
                </Grid>

                <!-- Tasks -->
                <Grid x:Name="TasksPanel" Margin="0,85,0,0" Visibility="Collapsed">
                    <!-- Add your full Tasks UI here (two column layout) -->
                    <TextBlock Text="Task Assistant Panel" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>

                <!-- Quiz -->
                <Grid x:Name="QuizPanel" Margin="0,85,0,0" Visibility="Collapsed">
                    <TextBlock Text="Quiz Game Panel" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>

                <!-- Log -->
                <Grid x:Name="LogPanel" Margin="0,85,0,0" Visibility="Collapsed">
                    <RichTextBox x:Name="rtbActivityLog" IsReadOnly="True" Background="#FF1E1E1E" Foreground="#90EE90" Padding="20"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
    